<Page
    x:Class="windowsappProject.Views.StartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:windowsappProject.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding StartPageInstance, Source={StaticResource Locator}}" 
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock Name="Title"  HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Title}" FontFamily="Segoe UI Historic"  />
            <TextBlock Name="SubTitle"  HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding SubTitle}" FontFamily="Segoe UI Historic" />
            <Button Margin="-3,5" x:Name="loginButton" Content="Log In" Command="{Binding SetLoginCommand}" />
        </StackPanel>
    </Grid>
</Page>
